{
  "mcpServers": {
    "rebebuca-debug": {
      "url": "http://127.0.0.1:3000/mcp/debug/sse",
      "description": "Rebebuca Debug MCP Server - backend logs (same port as web UI)"
    },
    "rebebuca": {
      "url": "http://127.0.0.1:3000/mcp/ai",
      "description": "Rebebuca AI MCP Server - task list and execution (same port as web UI)"
    }
  }
}
