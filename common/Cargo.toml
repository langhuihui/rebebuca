[package]
name = "rebebuca-common"
version = "0.1.0"
edition = "2021"
description = "Common types and utilities for Rebebuca projects"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.32"

[features]
default = []
# Enable system utilities (shell detection, port scanning, process stats)
system = []
