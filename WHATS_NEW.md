# Rebebuca 新版本重磅更新

距离上次更新已经过去一段时间，这段时间我们进行了密集的开发迭代。

**一周内发布了 20 个版本**，从 v0.2.0 一路更新到 v0.4.2，带来了大量令人兴奋的新功能！

让我们一起看看 Rebebuca 有哪些重大改进。

---

## 宏任务 - 一键启动整个开发环境

这是本次更新最重要的功能之一。

开发一个全栈项目，你可能需要同时启动：数据库、后端 API、前端开发服务器、文档服务...每次都要一个个点击运行？太麻烦了！

现在有了**宏任务**，你可以把多个任务组合成一个，一键全部启动。

### 两种执行模式

**串行执行** - 任务按顺序依次运行，前一个完成后才启动下一个。适合有依赖关系的场景：

```
启动开发环境（串行）
  1. 启动 Docker 容器
  2. 等待数据库就绪
  3. 运行数据库迁移
  4. 启动后端服务
  5. 启动前端服务
```

**并行执行** - 所有任务同时启动，快速拉起整个环境：

```
启动开发环境（并行）
  ├── 前端开发服务器
  ├── 后端 API 服务
  ├── Mock 服务器
  └── 文档预览服务
```

创建宏任务非常简单：新建任务时选择"宏任务"类型，然后选择要包含的子任务即可。

---

## 迷你模式 - 小巧随时待命

全新的迷你模式让 Rebebuca 可以只显示任务侧边栏，窗口变得非常紧凑。

- 占用最小屏幕空间
- 放在桌面角落不碍事
- 需要时随时展开

点击标题栏的切换按钮即可在正常模式和迷你模式之间切换。

---

## AI 编程工具集成

AI 编程已经成为开发者的日常，Rebebuca 现在内置了主流 AI CLI 编程工具的管理和快速启动功能。

### 支持的 AI 编程工具

| 工具 | 说明 |
|------|------|
| **Claude Code** | Anthropic 官方 CLI 编程助手 |
| **OpenAI Codex** | OpenAI 的 Codex CLI 工具 |
| **Google Gemini CLI** | Google 的 Gemini 命令行工具 |
| **GitHub Copilot CLI** | GitHub Copilot 命令行版本 |
| **Cursor CLI** | Cursor 编辑器的 CLI 代理 |
| **CodeBuddy** | 腾讯 AI 编程助手 |
| **Augment CLI (Auggie)** | Augment Code 命令行工具 |
| **OpenCode** | 开源 AI 编程助手 |
| **Qoder CLI** | Qodo AI 编程工具 |
| **Droid** | Factory AI 的 CLI 工具 |

### 功能特点

- **安装状态检测**: 自动检测工具是否已安装，显示版本号
- **一键安装引导**: 未安装的工具提供多种安装方式（npm、Homebrew、脚本等）
- **多提供商支持**: 支持配置不同的 API 提供商
  - 原厂 API (Original)
  - 智谱 AI (GLM)
  - 月之暗面 (Kimi)
  - 豆包 (Doubao)
  - MiniMax
  - DeepSeek
  - 自定义端点
- **API 密钥管理**: 统一管理各提供商的 API 密钥
- **快速启动**: 任务编辑对话框新增 AI 工具按钮，一键创建启动任务

---

## 系统终端选择

不喜欢内置终端？现在可以选择用系统安装的任何终端来运行任务。

### 支持的终端

**macOS**
- Terminal.app
- iTerm2
- Hyper
- Alacritty
- Kitty
- Warp

**Windows**
- Windows Terminal
- PowerShell
- CMD
- Hyper

**Linux**
- GNOME Terminal
- Konsole
- Alacritty
- Kitty
- Tilix

### 灵活配置

- 全局设置默认终端
- 每个任务可单独指定终端
- 自动检测已安装的终端

---

## 端口管理面板

端口管理功能从对话框升级为独立面板，体验更好。

- 实时显示所有监听端口
- 查看占用进程的详细信息（进程名、PID、完整命令行）
- 一键终止进程释放端口
- 支持端口筛选和搜索

Windows 用户特别优化：现在能显示完整的进程命令行，而不仅仅是可执行文件名。

---

## 通知面板

新增统一的通知管理面板：

- 集中查看所有通知
- 任务完成、失败等状态提醒
- 更新提示
- 不会错过任何重要信息

---

## 托盘菜单增强

系统托盘菜单变得更加实用：

### 最近任务

像 VSCode 的 Dock 菜单一样，托盘菜单现在显示最近运行的任务，点击即可快速重新运行。

### 运行状态指示

正在运行的任务会有明显的状态标识，一眼就能看到哪些服务在运行。

### 强制停止

对于卡住的任务，现在可以强制停止，确保能够终止顽固进程。

---

## 命令广场

新增命令广场功能，提供常用命令模板：

- 开发服务器命令
- 构建部署脚本
- Docker 常用命令
- Git 工作流
- 系统管理命令

新手可以从这里快速找到常用命令，老手也可以把自己的常用命令添加进来。

---

## 其他改进

### 命令输入简化

命令和参数合并为单一输入框，不再需要分开填写，更符合直觉。

### Python 环境支持

任务配置新增 Python 虚拟环境选项，自动激活指定的 venv 或 conda 环境。

### 管理员权限（Windows）

Windows 用户可以配置任务以管理员权限运行，适合需要提升权限的操作。

### 性能优化

- 启动速度优化
- 减少内存占用
- 大量任务时列表更流畅

### 环境变量继承

修复了 macOS/Linux 上环境变量继承问题，现在会正确加载 shell 配置（.bashrc、.zshrc 等）。

---

## 版本发布统计

| 时间 | 版本数 | 说明 |
|------|--------|------|
| 2025年10月 | 4 个 | v0.1.0 - v0.1.3，初始版本 |
| 2025年12月31日 | 3 个 | v0.1.4 - v0.1.6，功能完善 |
| 2026年1月2日-7日 | **20 个** | v0.1.7 - v0.4.2，密集迭代 |

**总计发布 27 个版本**，持续快速迭代中！

---

## 立即更新

访问 [rebebuca.com](https://rebebuca.com) 下载最新版本，或者在应用内检查更新。

如果你还没用过 Rebebuca，现在是最好的时机！

---

## 反馈与建议

使用中遇到问题或有好的建议？欢迎在 [GitHub Issues](https://github.com/langhuihui/rebebuca/issues) 告诉我们。

感谢每一位用户的支持，我们会继续努力让 Rebebuca 变得更好！
