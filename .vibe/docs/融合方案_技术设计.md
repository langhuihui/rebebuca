# FFmpegFreeUI 渐进式融合技术方案

> **文档版本**: 1.0
>
> **创建日期**: 2026-01-24
>
> **设计负责人**: 技术总监
>
> **文档类型**: 技术架构设计

---

## 一、设计概述

### 1.1 项目背景

将 FFmpegFreeUI (3FUI) 的核心视频转码功能集成到 Rebebuca 桌面应用中。3FUI 是一个功能丰富的 FFmpeg 前端工具，支持 40+ 视频编码器、20+ 音频编码器和复杂的视频滤镜处理。

### 1.2 技术挑战

| 挑战 | 说明 | 应对方案 |
|------|------|---------|
| **语言栈差异** | VB.NET vs TypeScript | 完全重写，借鉴功能而非代码 |
| **UI 框架差异** | WinForms vs Vue 3 | 重新设计 UI，参考布局 |
| **参数复杂性** | 180+ 个参数项 | 分阶段实现，优先核心功能 |
| **命令行生成** | 复杂的逻辑映射 | 提取算法，TypeScript 重写 |

### 1.3 设计原则

1. **渐进式融合**: 分 5 个阶段逐步实现，降低风险
2. **模块化设计**: 功能解耦，便于测试和维护
3. **兼容性优先**: 保持与 Rebebuca 现有架构的兼容性
4. **用户体验**: 简单模式 + 专家模式，满足不同用户需求

---

## 二、整体架构设计

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Rebebuca 主应用                          │
│                     (Vue 3 + Tauri 2)                         │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
┌───────────▼─────────┐  ┌─────────▼──────────┐  ┌──▼──────────────┐
│  FFmpeg 模块      │  │  Task Manager     │  │  Terminal Store │
│  (新增)            │  │  (现有)          │  │  (现有)        │
├───────────────────┤  ├──────────────────┤  ├────────────────┤
│ • 参数生成器      │  │ • 任务调度       │  │ • PTY 管理     │
│ • 进度解析器      │  │ • 执行管理       │  │ • 输出处理     │
│ • 预设管理器      │  │ • 历史记录       │  │ • 状态监控     │
│ • 编码器数据库     │  └──────────────────┘  └────────────────┘
└───────────────────┘              │
            │                      │
            └──────────┬───────────┘
                       │
                ┌──────▼──────┐
                │  Tauri     │
                │  后端      │
                └────────────┘
```

### 2.2 模块划分

#### 2.2.1 核心模块

```
src/ffmpeg/
├── stores/
│   ├── ffmpegParams.ts      # FFmpeg 参数状态管理
│   ├── ffmpegPresets.ts    # 预设管理
│   └── ffmpegEncoder.ts    # 编码器数据库
├── services/
│   ├── commandBuilder.ts    # 命令行生成服务
│   ├── progressParser.ts    # 进度解析服务
│   └── encoderDatabase.ts   # 编码器参数数据库
├── types/
│   ├── preset.ts          # 预设类型定义
│   ├── encoder.ts         # 编码器类型定义
│   └── filter.ts         # 滤镜类型定义
├── components/
│   ├── FFmpegConfigPanel.vue       # 主配置面板
│   ├── VideoEncoderTab.vue        # 视频编码器选项卡
│   ├── AudioEncoderTab.vue        # 音频编码器选项卡
│   ├── FilterPanel.vue           # 滤镜面板
│   ├── PresetManager.vue         # 预设管理器
│   ├── ProgressMonitor.vue       # 进度监控
│   └── CommandPreview.vue       # 命令行预览
├── utils/
│   ├── time.ts             # 时间工具函数
│   ├── path.ts            # 路径工具函数
│   └── validators.ts      # 参数验证工具
└── data/
    └── encoders.ts       # 编码器数据库 (JSON)
```

#### 2.2.2 与现有模块的集成

```
stores/taskManager.ts (修改)
├── 扩展 Task 类型，支持 FFmpeg 配置
├── 添加 FFmpeg 任务类型 (FFmpegTask)
└── 集成 FFmpeg 进度解析

stores/terminal.ts (修改)
├── 扩展 Tab 类型，支持 FFmpeg 进度
└── 集成 FFmpeg 进度事件

adapters/tauri.ts (保持不变)
└── 使用现有的 PTY 执行机制
```

---

## 三、核心接口设计

### 3.1 TypeScript 类型定义

#### 3.1.1 预设数据结构

```typescript
// src/ffmpeg/types/preset.ts

/**
 * FFmpeg 预设数据结构
 * 参考自 FFmpegFreeUI 的预设数据类型.vb
 */
export interface FFmpegPreset {
  // ==================== 输出配置 ====================
  output: {
    container: string;              // 输出容器 (mp4, mkv, avi, etc.)
    naming: {
      useAutoNaming: boolean;      // 是否使用自动命名
      autoNamingOption: number;     // 自动命名选项
      prefix: string;             // 前缀
      suffix: string;             // 后缀
      customPattern: string;      // 自定义命名模式
    };
    location?: string;              // 输出位置 (默认同输入目录)
  };

  // ==================== 解码参数 ====================
  decoder: {
    decoder: string;              // 解码器 (auto, h264_cuvid, h264_qsv, etc.)
    hwaccel: string;             // 硬件加速 (auto, cuda, qsv, d3d11va, etc.)
    hwaccelDevice?: string;       // 硬件加速设备
  };

  // ==================== 视频编码器 ====================
  video: {
    enabled: boolean;            // 是否处理视频
    encoderCategory: string;      // 编码器类别 (h264, hevc, av1, vp9, etc.)
    encoder: string;            // 具体编码器 (libx264, libx265, etc.)
    preset: string;             // 编码预设 (ultrafast, fast, medium, slow, etc.)
    profile: string;           // 编码配置文件 (baseline, main, high, etc.)
    level: string;             // 编码级别 (3.0, 3.1, 4.0, etc.)
    tune: string;             // 调整参数 (film, animation, grain, etc.)
    passMode: number;          // 编码方式 (0=单次, 1=首次, 2=二次)
    passLogFile?: string;      // 二次编码日志文件
  };

  // ==================== 质量控制 ====================
  quality: {
    controlMode: 'CRF' | 'VBR' | 'VBR_HQ' | 'CQP' | 'CBR';
    paramName: 'crf' | 'cq' | 'qp' | 'global_quality';
    value: string;
    bitrate: {
      base: string;            // 目标比特率 (例如: 5M)
      min?: string;           // 最小比特率
      max?: string;           // 最大比特率
      bufferSize?: string;     // 缓冲区大小 (例如: 10M)
    };
  };

  // ==================== 视频滤镜 ====================
  filters: {
    // 裁剪
    crop?: {
      enabled: boolean;
      width: string;
      height: string;
      x: string;
      y: string;
    };

    // 缩放
    scale?: {
      enabled: boolean;
      width: string;           // 目标宽度 (例如: 1920, iw/2)
      height: string;          // 目标高度 (例如: 1080, ih/2)
      keepAspect: boolean;     // 保持宽高比
      algorithm?: string;      // 缩放算法 (bicubic, lanczos, etc.)
    };

    // 帧率
    framerate?: {
      enabled: boolean;
      fps: string;            // 目标帧率 (例如: 30, 29.97)
      mode?: number;          // 帧率转换模式
    };

    // 去隔行
    deinterlace?: {
      enabled: boolean;
      mode: number;           // 去隔行模式
    };

    // 降噪
    denoise?: {
      enabled: boolean;
      mode: string;          // 降噪模式
      strength: string;       // 降噪强度
    };

    // 锐化
    sharpen?: {
      enabled: boolean;
      strength: string;       // 锐化强度
    };

    // 字幕烧录
    subtitle?: {
      enabled: boolean;
      source: 'embedded' | 'external';
      file?: string;         // 外部字幕文件路径
      streamIndex?: number;   // 内置字幕流索引
      styling?: SubtitleStyling;
    };

    // 色彩管理
    colorManagement?: {
      enabled: boolean;
      pixelFormat: string;    // 像素格式 (yuv420p, yuv422p, etc.)
      colorSpace: string;     // 色彩空间 (bt709, bt2020, etc.)
      transfer: string;       // 传输特性 (bt709, smpte2084, etc.)
      primaries: string;      // 原色 (bt709, bt2020, etc.)
      range?: string;        // 色彩范围 (tv, pc, limited, full)
    };

    // 旋转/翻转
    transform?: {
      enabled: boolean;
      rotation: string;       // 旋转角度 (0, 90, 180, 270)
      flipH?: boolean;        // 水平翻转
      flipV?: boolean;        // 垂直翻转
    };
  };

  // ==================== 音频编码 ====================
  audio: {
    enabled: boolean;            // 是否处理音频
    encoder: string;            // 编码器 (aac, libopus, libmp3lame, etc.)
    bitrate: string;            // 比特率 (192k, 128k, etc.)
    channels: string;           // 声道数 (1, 2, 6, etc.)
    sampleRate: string;         // 采样率 (44100, 48000, etc.)
    codec?: string;            // 强制指定音频编解码器
  };

  // ==================== 剪辑区间 ====================
  trimming: {
    enabled: boolean;
    startTime: string;         // 开始时间 (HH:MM:SS.mmm)
    endTime: string;           // 结束时间 (HH:MM:SS.mmm)
  };

  // ==================== 流控制 ====================
  streamControl: {
    keepOtherVideoStreams: boolean;   // 保留其他视频流
    keepOtherAudioStreams: boolean;   // 保留其他音频流
    keepSubtitleStreams: boolean;     // 保留字幕流
    keepAttachmentStreams: boolean;   // 保留附件流
    mapAll?: boolean;                // 映射所有流
  };

  // ==================== 元数据 ====================
  metadata?: {
    title?: string;
    artist?: string;
    album?: string;
    year?: string;
    comment?: string;
  };

  // ==================== 自定义参数 ====================
  custom: {
    preParams: string;          // 输入前的参数
    videoFilter: string;        // 自定义视频滤镜
    audioFilter: string;        // 自定义音频滤镜
    videoParams: string;        // 自定义视频参数
    audioParams: string;        // 自定义音频参数
    postParams: string;         // 输出后的参数
    fullCustom: string;         // 完全自定义命令行
  };
}

/**
 * 字幕样式配置
 */
export interface SubtitleStyling {
  forceStyle: string;          // 字幕样式字符串
  fontSize?: string;          // 字体大小
  fontName?: string;          // 字体名称
  primaryColor?: string;      // 主颜色
  backColor?: string;         // 背景颜色
  outlineColor?: string;       // 边框颜色
  alignment?: number;         // 对齐方式
}

/**
 * 预设元数据
 */
export interface PresetMetadata {
  id: string;
  name: string;
  description?: string;
  version: string;          // 预设格式版本
  createdAt: number;
  updatedAt: number;
  author?: string;
  tags?: string[];
}
```

#### 3.1.2 编码器数据库结构

```typescript
// src/ffmpeg/types/encoder.ts

/**
 * 编码器类别
 */
export type EncoderCategory =
  | 'h264'
  | 'hevc'
  | 'av1'
  | 'vp9'
  | 'mpeg2'
  | 'mpeg4'
  | 'prores'
  | 'dnxhd'
  | 'ffv1'
  | 'copy';

/**
 * 编码器信息
 */
export interface EncoderInfo {
  id: string;                // 编码器 ID
  category: EncoderCategory;   // 类别
  name: string;              // 显示名称
  ffmpegName: string;        // FFmpeg 编码器名称
  description?: string;       // 描述

  // 支持的预设
  presets?: string[];         // 例如: ['ultrafast', 'fast', 'medium', 'slow', 'veryslow']

  // 支持的配置文件
  profiles?: string[];        // 例如: ['baseline', 'main', 'high', 'high10']

  // 支持的级别
  levels?: string[];          // 例如: ['3.0', '3.1', '4.0', '4.1', '4.2']

  // 支持的调整参数
  tunes?: string[];           // 例如: ['film', 'animation', 'grain', 'stillimage']

  // 质量控制方式
  qualityModes?: ('CRF' | 'VBR' | 'VBR_HQ' | 'CQP' | 'CBR')[];

  // 硬件加速支持
  hwaccel?: string[];         // 支持的硬件加速

  // 是否支持二遍编码
  supportsPassEncoding?: boolean;

  // 是否支持色彩管理
  supportsColorManagement?: boolean;

  // 推荐预设配置
  recommendedSettings?: Partial<FFmpegPreset>;
}

/**
 * 音频编码器信息
 */
export type AudioEncoderCategory =
  | 'aac'
  | 'opus'
  | 'mp3'
  | 'flac'
  | 'vorbis'
  | 'ac3'
  | 'eac3'
  | 'dts'
  | 'copy';

export interface AudioEncoderInfo {
  id: string;
  category: AudioEncoderCategory;
  name: string;
  ffmpegName: string;
  description?: string;

  // 支持的比特率
  bitrates?: string[];       // 例如: ['64k', '128k', '192k', '256k', '320k']

  // 支持的声道数
  channelCounts?: number[];   // 例如: [1, 2, 6]

  // 支持的采样率
  sampleRates?: number[];      // 例如: [44100, 48000, 96000]

  // 是否支持 VBR
  supportsVBR?: boolean;
}
```

#### 3.1.3 进度数据结构

```typescript
// src/ffmpeg/types/progress.ts

/**
 * FFmpeg 进度信息
 */
export interface FFmpegProgress {
  // 基础信息
  frame?: number;             // 当前帧数
  fps?: number;              // 帧率
  q?: number;               // 量化参数

  // 时间信息
  time?: string;             // 当前时间 (HH:MM:SS.mmm)
  duration?: string;         // 总时长 (HH:MM:SS.mmm)
  timeSeconds?: number;      // 当前时间 (秒)
  durationSeconds?: number;  // 总时长 (秒)

  // 大小信息
  size?: number;            // 当前大小
  sizeUnit?: 'KiB' | 'MiB' | 'GiB';
  sizeBytes?: number;       // 大小 (字节)

  // 比特率
  bitrate?: number;         // 当前比特率 (kbits/s)
  estimatedSize?: number;   // 预估文件大小 (字节)

  // 速度
  speed?: number;           // 处理速度 (倍数)

  // 进度
  progress?: number;        // 进度百分比 (0-100)

  // 剩余时间
  remainingTime?: number;   // 剩余时间 (秒)

  // 状态
  status?: 'analyzing' | 'encoding' | 'muxing' | 'finished' | 'error';

  // 错误信息
  error?: string;
}

/**
 * 进度事件类型
 */
export type FFmpegProgressEvent =
  | { type: 'progress'; data: FFmpegProgress }
  | { type: 'duration'; duration: string; durationSeconds: number }
  | { type: 'complete'; exitCode: number }
  | { type: 'error'; error: string }
  | { type: 'warning'; warning: string };
```

### 3.2 Pinia Store 设计

#### 3.2.1 FFmpeg 参数 Store

```typescript
// src/ffmpeg/stores/ffmpegParams.ts

import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import type { FFmpegPreset } from '../types/preset';
import { commandBuilder } from '../services/commandBuilder';

export const useFFmpegParamsStore = defineStore('ffmpegParams', () => {
  // ==================== State ====================

  // 当前预设
  const currentPreset = ref<FFmpegPreset>(createDefaultPreset());

  // 输入文件列表
  const inputFiles = ref<string[]>([]);

  // 输出文件 (单个或批量)
  const outputFile = ref<string>('');

  // 预览模式
  const previewMode = ref<'simple' | 'expert'>('simple');

  // 命令行预览
  const commandPreview = ref<string>('');

  // 是否正在生成命令
  const isGeneratingCommand = ref(false);

  // ==================== Computed ====================

  // 是否有输入文件
  const hasInputFiles = computed(() => inputFiles.value.length > 0);

  // 输入文件数量
  const inputCount = computed(() => inputFiles.value.length);

  // 是否批量处理
  const isBatchMode = computed(() => inputFiles.value.length > 1);

  // 当前配置是否有效
  const isValid = computed(() => {
    return hasInputFiles.value && currentPreset.value.output.container;
  });

  // ==================== Actions ====================

  /**
   * 添加输入文件
   */
  function addInputFiles(files: string[]) {
    inputFiles.value.push(...files);
    updateCommandPreview();
  }

  /**
   * 移除输入文件
   */
  function removeInputFile(index: number) {
    inputFiles.value.splice(index, 1);
    updateCommandPreview();
  }

  /**
   * 清空输入文件
   */
  function clearInputFiles() {
    inputFiles.value = [];
    updateCommandPreview();
  }

  /**
   * 更新预设
   */
  function updatePreset(updates: Partial<FFmpegPreset>) {
    currentPreset.value = {
      ...currentPreset.value,
      ...updates,
    };
    updateCommandPreview();
  }

  /**
   * 重置预设为默认值
   */
  function resetPreset() {
    currentPreset.value = createDefaultPreset();
    updateCommandPreview();
  }

  /**
   * 应用预设
   */
  async function applyPreset(preset: FFmpegPreset) {
    currentPreset.value = JSON.parse(JSON.stringify(preset));
    await updateCommandPreview();
  }

  /**
   * 生成命令行预览
   */
  async function updateCommandPreview() {
    if (!hasInputFiles.value) {
      commandPreview.value = '';
      return;
    }

    isGeneratingCommand.value = true;

    try {
      const inputFile = inputFiles.value[0];
      const outputFile = generateOutputPath(inputFile, currentPreset.value);
      commandPreview.value = await commandBuilder.build(currentPreset.value, inputFile, outputFile);
    } catch (error) {
      console.error('[FFmpegParams] Failed to build command:', error);
      commandPreview.value = `Error: ${error instanceof Error ? error.message : String(error)}`;
    } finally {
      isGeneratingCommand.value = false;
    }
  }

  /**
   * 生成输出路径
   */
  function generateOutputPath(inputFile: string, preset: FFmpegPreset): string {
    // 实现输出路径生成逻辑
    // ...
  }

  /**
   * 创建默认预设
   */
  function createDefaultPreset(): FFmpegPreset {
    return {
      output: {
        container: 'mp4',
        naming: {
          useAutoNaming: true,
          autoNamingOption: 0,
          prefix: '',
          suffix: '',
          customPattern: '',
        },
      },
      decoder: {
        decoder: 'auto',
        hwaccel: 'auto',
      },
      video: {
        enabled: true,
        encoderCategory: 'h264',
        encoder: 'libx264',
        preset: 'medium',
        profile: 'high',
        level: '4.0',
        tune: '',
        passMode: 0,
      },
      quality: {
        controlMode: 'CRF',
        paramName: 'crf',
        value: '23',
        bitrate: {
          base: '5M',
        },
      },
      filters: {},
      audio: {
        enabled: true,
        encoder: 'aac',
        bitrate: '192k',
        channels: '2',
        sampleRate: '48000',
      },
      trimming: {
        enabled: false,
        startTime: '',
        endTime: '',
      },
      streamControl: {
        keepOtherVideoStreams: false,
        keepOtherAudioStreams: false,
        keepSubtitleStreams: false,
        keepAttachmentStreams: false,
      },
      custom: {
        preParams: '',
        videoFilter: '',
        audioFilter: '',
        videoParams: '',
        audioParams: '',
        postParams: '',
        fullCustom: '',
      },
    };
  }

  return {
    // State
    currentPreset,
    inputFiles,
    outputFile,
    previewMode,
    commandPreview,
    isGeneratingCommand,

    // Computed
    hasInputFiles,
    inputCount,
    isBatchMode,
    isValid,

    // Actions
    addInputFiles,
    removeInputFile,
    clearInputFiles,
    updatePreset,
    resetPreset,
    applyPreset,
    updateCommandPreview,
  };
});
```

#### 3.2.2 预设管理 Store

```typescript
// src/ffmpeg/stores/ffmpegPresets.ts

import { defineStore } from 'pinia';
import { ref } from 'vue';
import type { FFmpegPreset, PresetMetadata } from '../types/preset';
import { safeInvoke } from '../../utils/programUtils';

export const useFFmpegPresetsStore = defineStore('ffmpegPresets', () => {
  // ==================== State ====================

  // 预设列表
  const presets = ref<Map<string, { preset: FFmpegPreset; meta: PresetMetadata }>>(new Map());

  // 内置预设模板
  const builtinPresets = ref<FFmpegPreset[]>([]);

  // 当前选中的预设 ID
  const selectedPresetId = ref<string | null>(null);

  // ==================== Actions ====================

  /**
   * 加载内置预设
   */
  async function loadBuiltinPresets() {
    // 加载内置预设模板
    builtinPresets.value = await importBuiltinPresets();
  }

  /**
   * 保存预设
   */
  async function savePreset(preset: FFmpegPreset, name: string, description?: string) {
    const meta: PresetMetadata = {
      id: `preset-${Date.now()}`,
      name,
      description,
      version: '1.0',
      createdAt: Date.now(),
      updatedAt: Date.now(),
      author: '',
    };

    const key = `ffmpeg.preset.${meta.id}`;
    const value = JSON.stringify({ preset, meta });

    // 通过 Tauri 存储保存
    await safeInvoke('save_preset', { key, value });

    presets.value.set(meta.id, { preset, meta });
  }

  /**
   * 加载预设
   */
  async function loadPreset(id: string) {
    const key = `ffmpeg.preset.${id}`;
    const value = await safeInvoke<string>('load_preset', { key });

    if (value) {
      const { preset, meta } = JSON.parse(value);
      presets.value.set(id, { preset, meta });
      return preset;
    }

    return null;
  }

  /**
   * 删除预设
   */
  async function deletePreset(id: string) {
    const key = `ffmpeg.preset.${id}`;
    await safeInvoke('delete_preset', { key });
    presets.value.delete(id);
  }

  /**
   * 导出预设到文件
   */
  async function exportPresetToFile(preset: FFmpegPreset, filePath: string) {
    const content = JSON.stringify(preset, null, 2);
    await safeInvoke('write_text_file', { path: filePath, content });
  }

  /**
   * 从文件导入预设
   */
  async function importPresetFromFile(filePath: string): Promise<FFmpegPreset> {
    const content = await safeInvoke<string>('read_text_file', { path: filePath });
    return JSON.parse(content);
  }

  /**
   * 从 3FUI 格式导入
   */
  async function importFrom3FUI(filePath: string): Promise<FFmpegPreset> {
    const content = await safeInvoke<string>('read_text_file', { path: filePath });
    const oldPreset = JSON.parse(content);

    // 映射字段名
    return convert3FUIToRebebuca(oldPreset);
  }

  /**
   * 转换 3FUI 格式到 Rebebuca 格式
   */
  function convert3FUIToRebebuca(old: any): FFmpegPreset {
    return {
      output: {
        container: old.输出容器 || '',
        naming: {
          useAutoNaming: old.输出命名_使用自动命名 || false,
          autoNamingOption: old.输出命名_自动命名选项 || 0,
          prefix: old.输出命名_前缀 || '',
          suffix: old.输出命名_后缀 || '',
          customPattern: old.输出命名_自定义模式 || '',
        },
      },
      video: {
        enabled: true,
        encoderCategory: old.视频参数_编码器_类别 || '',
        encoder: old.视频参数_编码器_具体编码 || '',
        preset: old.视频参数_编码器_编码预设 || '',
        profile: old.视频参数_编码器_配置文件 || '',
        level: old.视频参数_编码器_级别 || '',
        tune: old.视频参数_编码器_调整 || '',
        passMode: old.视频参数_编码器_编码方式 || 0,
      },
      // ... 更多字段映射
    };
  }

  /**
   * 初始化加载所有预设
   */
  async function initialize() {
    await loadBuiltinPresets();

    // 加载用户预设
    const presetKeys = await safeInvoke<string[]>('list_preset_keys', { prefix: 'ffmpeg.preset.' });

    for (const key of presetKeys) {
      const id = key.replace('ffmpeg.preset.', '');
      await loadPreset(id);
    }
  }

  return {
    // State
    presets,
    builtinPresets,
    selectedPresetId,

    // Actions
    savePreset,
    loadPreset,
    deletePreset,
    exportPresetToFile,
    importPresetFromFile,
    importFrom3FUI,
    initialize,
  };
});
```

### 3.3 命令行生成服务

```typescript
// src/ffmpeg/services/commandBuilder.ts

import type { FFmpegPreset } from '../types/preset';
import { escapeShellArg } from '../utils/path';

/**
 * FFmpeg 命令行生成器
 * 参考自 FFmpegFreeUI/编码任务/预设管理.vb
 */
export class CommandBuilder {
  /**
   * 构建完整的 FFmpeg 命令行
   */
  async build(preset: FFmpegPreset, inputFile: string, outputFile: string): Promise<string> {
    const args: string[] = [];

    // ==================== 全局参数 ====================
    args.push('-hide_banner', '-nostdin', '-y'); // -y: 覆盖输出文件

    // ==================== 解码参数 ====================
    if (preset.decoder.decoder && preset.decoder.decoder !== 'auto') {
      args.push('-hwaccel', preset.decoder.hwaccel);
    }
    if (preset.decoder.hwaccelDevice) {
      args.push('-hwaccel_device', preset.decoder.hwaccelDevice);
    }

    // ==================== 剪辑区间 ====================
    if (preset.trimming.enabled) {
      args.push('-ss', preset.trimming.startTime);
      if (preset.trimming.endTime) {
        args.push('-to', preset.trimming.endTime);
      }
    }

    // ==================== 自定义前置参数 ====================
    if (preset.custom.preParams) {
      args.push(...this.parseCustomParams(preset.custom.preParams));
    }

    // ==================== 输入文件 ====================
    args.push('-i', escapeShellArg(inputFile));

    // ==================== 视频编码参数 ====================
    if (preset.video.enabled) {
      const videoArgs = await this.buildVideoArgs(preset);
      args.push(...videoArgs);
    }

    // ==================== 音频编码参数 ====================
    if (preset.audio.enabled) {
      const audioArgs = this.buildAudioArgs(preset);
      args.push(...audioArgs);
    }

    // ==================== 流映射 ====================
    const mapArgs = this.buildStreamMap(preset);
    args.push(...mapArgs);

    // ==================== 自定义后置参数 ====================
    if (preset.custom.postParams) {
      args.push(...this.parseCustomParams(preset.custom.postParams));
    }

    // ==================== 输出文件 ====================
    args.push(escapeShellArg(outputFile));

    return args.join(' ');
  }

  /**
   * 构建视频编码参数
   */
  private async buildVideoArgs(preset: FFmpegPreset): Promise<string[]> {
    const args: string[] = [];

    // 编码器
    if (preset.video.encoder) {
      args.push('-c:v', preset.video.encoder);
    }

    // 预设
    if (preset.video.preset) {
      args.push('-preset', preset.video.preset);
    }

    // 配置文件
    if (preset.video.profile) {
      args.push('-profile:v', preset.video.profile);
    }

    // 级别
    if (preset.video.level) {
      args.push('-level', preset.video.level);
    }

    // 调整
    if (preset.video.tune) {
      args.push('-tune', preset.video.tune);
    }

    // 质量控制
    const qualityArgs = this.buildQualityArgs(preset.quality);
    args.push(...qualityArgs);

    // 视频滤镜
    const filters = this.buildVideoFilters(preset);
    if (filters.length > 0) {
      args.push('-vf', filters.join(','));
    }

    // 自定义视频参数
    if (preset.custom.videoParams) {
      args.push(...this.parseCustomParams(preset.custom.videoParams));
    }

    return args;
  }

  /**
   * 构建质量控制参数
   */
  private buildQualityArgs(quality: FFmpegPreset['quality']): string[] {
    const args: string[] = [];

    switch (quality.controlMode) {
      case 'CRF':
        args.push(`-${quality.paramName}`, quality.value);
        break;

      case 'VBR':
      case 'VBR_HQ':
        if (quality.bitrate.base) {
          args.push('-b:v', quality.bitrate.base);
        }
        if (quality.bitrate.min) {
          args.push('-minrate', quality.bitrate.min);
        }
        if (quality.bitrate.max) {
          args.push('-maxrate', quality.bitrate.max);
        }
        if (quality.bitrate.bufferSize) {
          args.push('-bufsize', quality.bitrate.bufferSize);
        }
        break;

      case 'CQP':
        args.push(`-${quality.paramName}`, quality.value);
        break;

      case 'CBR':
        if (quality.bitrate.base) {
          args.push('-b:v', quality.bitrate.base);
          args.push('-maxrate', quality.bitrate.base);
          args.push('-minrate', quality.bitrate.base);
          args.push('-bufsize', quality.bitrate.bufferSize || '2M');
        }
        break;
    }

    return args;
  }

  /**
   * 构建视频滤镜
   */
  private buildVideoFilters(preset: FFmpegPreset): string[] {
    const filters: string[] = [];
    const f = preset.filters;

    // 裁剪
    if (f.crop?.enabled) {
      filters.push(`crop=${f.crop.width}:${f.crop.height}:${f.crop.x}:${f.crop.y}`);
    }

    // 缩放
    if (f.scale?.enabled) {
      if (f.scale.keepAspect) {
        // 保持宽高比
        filters.push(`scale=${f.scale.width}:-1`);
      } else {
        filters.push(`scale=${f.scale.width}:${f.scale.height}`);
      }
    }

    // 帧率
    if (f.framerate?.enabled) {
      filters.push(`fps=${f.framerate.fps}`);
    }

    // 去隔行
    if (f.deinterlace?.enabled) {
      const modes = {
        1: 'yadif=0:-1:0',
        2: 'yadif=0:0:0',
        3: 'yadif=0:1:0',
        4: 'tinterlace=4',
        5: 'tinterlace=6',
        6: 'fieldmatch,yadif=deint=interlaced,decimate',
        7: 'yadif=1',
      };
      filters.push(modes[f.deinterlace.mode] || 'yadif');
    }

    // 字幕烧录
    if (f.subtitle?.enabled) {
      if (f.subtitle.source === 'external') {
        filters.push(`subtitles=${escapeShellArg(f.subtitle.file!)}`);
      } else if (f.subtitle.streamIndex !== undefined) {
        filters.push(`subtitles=${f.subtitle.streamIndex}:force_style='${f.subtitle.styling?.forceStyle || ''}'`);
      }
    }

    // 色彩管理
    if (f.colorManagement?.enabled) {
      if (f.colorManagement.pixelFormat) {
        filters.push(`format=${f.colorManagement.pixelFormat}`);
      }
      if (f.colorManagement.colorSpace) {
        filters.push(`colormatrix=${f.colorManagement.colorSpace}`);
      }
    }

    // 旋转/翻转
    if (f.transform?.enabled) {
      if (f.transform.rotation && f.transform.rotation !== '0') {
        filters.push(`rotate=${f.transform.rotation}*PI/180`);
      }
      if (f.transform.flipH) {
        filters.push('hflip');
      }
      if (f.transform.flipV) {
        filters.push('vflip');
      }
    }

    // 自定义滤镜
    if (preset.custom.videoFilter) {
      filters.push(preset.custom.videoFilter);
    }

    return filters;
  }

  /**
   * 构建音频编码参数
   */
  private buildAudioArgs(preset: FFmpegPreset): string[] {
    const args: string[] = [];

    // 编码器
    if (preset.audio.encoder) {
      args.push('-c:a', preset.audio.encoder);
    }

    // 比特率
    if (preset.audio.bitrate) {
      args.push('-b:a', preset.audio.bitrate);
    }

    // 声道数
    if (preset.audio.channels) {
      args.push('-ac', preset.audio.channels);
    }

    // 采样率
    if (preset.audio.sampleRate) {
      args.push('-ar', preset.audio.sampleRate);
    }

    // 自定义音频参数
    if (preset.custom.audioParams) {
      args.push(...this.parseCustomParams(preset.custom.audioParams));
    }

    return args;
  }

  /**
   * 构建流映射参数
   */
  private buildStreamMap(preset: FFmpegPreset): string[] {
    const args: string[] = [];

    if (preset.streamControl.mapAll) {
      // 映射所有流
      args.push('-map', '0');
    } else {
      // 选择性映射
      if (preset.video.enabled) {
        args.push('-map', '0:v:0');  // 主视频流
      }
      if (preset.audio.enabled) {
        args.push('-map', '0:a:0');  // 主音频流
      }
      if (preset.streamControl.keepSubtitleStreams) {
        args.push('-map', '0:s?');  // 所有字幕流
      }
    }

    return args;
  }

  /**
   * 解析自定义参数
   */
  private parseCustomParams(params: string): string[] {
    // 分割参数并过滤空字符串
    return params
      .split(/\s+/)
      .filter(p => p.trim().length > 0);
  }

  /**
   * 生成批量命令
   */
  async buildBatch(preset: FFmpegPreset, inputFiles: string[]): Promise<string[]> {
    const commands: string[] = [];

    for (const inputFile of inputFiles) {
      const outputFile = this.generateOutputPath(inputFile, preset);
      const command = await this.build(preset, inputFile, outputFile);
      commands.push(command);
    }

    return commands;
  }

  /**
   * 生成输出路径
   */
  private generateOutputPath(inputFile: string, preset: FFmpegPreset): string {
    const path = require('path');
    const parsed = path.parse(inputFile);

    if (preset.output.naming.useAutoNaming) {
      // 自动命名
      let name = parsed.name;

      // 添加前缀
      if (preset.output.naming.prefix) {
        name = preset.output.naming.prefix + name;
      }

      // 添加后缀
      if (preset.output.naming.suffix) {
        name += preset.output.naming.suffix;
      }

      // 自定义模式 (高级功能)
      if (preset.output.naming.customPattern) {
        name = this.applyCustomNamingPattern(
          preset.output.naming.customPattern,
          parsed.name,
          parsed.dir
        );
      }

      return path.join(parsed.dir, `${name}.${preset.output.container}`);
    } else {
      // 使用指定位置
      return preset.output.location || path.join(parsed.dir, `${parsed.name}.${preset.output.container}`);
    }
  }

  /**
   * 应用自定义命名模式
   */
  private applyCustomNamingPattern(pattern: string, filename: string, dir: string): string {
    // 替换占位符
    const now = new Date();
    return pattern
      .replace('{name}', filename)
      .replace('{date}', now.toISOString().split('T')[0])
      .replace('{time}', now.toTimeString().split(' ')[0])
      .replace('{timestamp}', String(now.getTime()));
  }
}

// 导出单例
export const commandBuilder = new CommandBuilder();
```

### 3.4 进度解析服务

```typescript
// src/ffmpeg/services/progressParser.ts

import type { FFmpegProgress, FFmpegProgressEvent } from '../types/progress';

/**
 * FFmpeg 进度解析器
 * 参考自 FFmpegFreeUI/编码任务/编码任务.vb
 */
export class ProgressParser {
  private duration: number | null = null;
  private progress: Partial<FFmpegProgress> = {};

  // 正则表达式模式
  private readonly patterns = {
    duration: /Duration:\s*(\d{2}:\d{2}:\d{2}\.\d{2})/,
    frame: /frame=\s*(\d+)/,
    fps: /fps=\s*(\d+)/,
    q: /q=\s*([\d\.]+)/,
    size: /size=\s*(\d+)\s*([KMG]iB)/,
    time: /time=\s*(\d{2}:\d{2}:\d{2}\.\d{2})/,
    bitrate: /bitrate:\s*([\d\.]+)\s*kbits\/s/,
    speed: /speed=\s*([\d\.eE\+\-]+)\s*x/,
    error: /Error\s+(.+)/,
    warning: /Warning\s+(.+)/,
  };

  /**
   * 解析一行输出
   */
  parse(line: string): FFmpegProgressEvent | null {
    // 解析时长
    const durationMatch = line.match(this.patterns.duration);
    if (durationMatch) {
      this.duration = this.parseTimeString(durationMatch[1]);
      return {
        type: 'duration',
        duration: durationMatch[1],
        durationSeconds: this.duration,
      };
    }

    // 解析错误
    const errorMatch = line.match(this.patterns.error);
    if (errorMatch) {
      return {
        type: 'error',
        error: errorMatch[1],
      };
    }

    // 解析警告
    const warningMatch = line.match(this.patterns.warning);
    if (warningMatch) {
      return {
        type: 'warning',
        warning: warningMatch[1],
      };
    }

    // 解析进度
    const progressData = this.extractProgressData(line);
    if (progressData) {
      this.progress = { ...this.progress, ...progressData };

      // 计算进度百分比
      if (this.progress.time && this.duration) {
        this.progress.progress = Math.min((this.progress.timeSeconds! / this.duration) * 100, 100);

        // 计算预估大小
        if (this.progress.sizeBytes && this.progress.progress) {
          this.progress.estimatedSize = this.progress.sizeBytes / (this.progress.progress / 100);
        }

        // 计算剩余时间
        if (this.progress.speed && this.progress.timeSeconds) {
          const remaining = (this.duration - this.progress.timeSeconds) / this.progress.speed;
          this.progress.remainingTime = Math.max(0, remaining);
        }
      }

      return {
        type: 'progress',
        data: this.progress as FFmpegProgress,
      };
    }

    return null;
  }

  /**
   * 提取进度数据
   */
  private extractProgressData(line: string): Partial<FFmpegProgress> | null {
    const data: Partial<FFmpegProgress> = {};
    let hasData = false;

    // 帧
    const frameMatch = line.match(this.patterns.frame);
    if (frameMatch) {
      data.frame = parseInt(frameMatch[1]);
      hasData = true;
    }

    // 帧率
    const fpsMatch = line.match(this.patterns.fps);
    if (fpsMatch) {
      data.fps = parseInt(fpsMatch[1]);
      hasData = true;
    }

    // 量化参数
    const qMatch = line.match(this.patterns.q);
    if (qMatch) {
      data.q = parseFloat(qMatch[1]);
      hasData = true;
    }

    // 大小
    const sizeMatch = line.match(this.patterns.size);
    if (sizeMatch) {
      data.size = parseInt(sizeMatch[1]);
      data.sizeUnit = sizeMatch[2] as 'KiB' | 'MiB' | 'GiB';
      data.sizeBytes = this.convertSizeToBytes(data.size, data.sizeUnit);
      hasData = true;
    }

    // 时间
    const timeMatch = line.match(this.patterns.time);
    if (timeMatch) {
      data.time = timeMatch[1];
      data.timeSeconds = this.parseTimeString(timeMatch[1]);
      hasData = true;
    }

    // 比特率
    const bitrateMatch = line.match(this.patterns.bitrate);
    if (bitrateMatch) {
      data.bitrate = parseFloat(bitrateMatch[1]);
      hasData = true;
    }

    // 速度
    const speedMatch = line.match(this.patterns.speed);
    if (speedMatch) {
      data.speed = parseFloat(speedMatch[1]);
      hasData = true;
    }

    return hasData ? data : null;
  }

  /**
   * 解析时间字符串
   * 格式: HH:MM:SS.mmm
   */
  private parseTimeString(timeStr: string): number {
    const [hours, minutes, seconds] = timeStr.split(':').map(parseFloat);
    return hours * 3600 + minutes * 60 + seconds;
  }

  /**
   * 转换大小到字节
   */
  private convertSizeToBytes(value: number, unit: 'KiB' | 'MiB' | 'GiB'): number {
    switch (unit) {
      case 'KiB':
        return value * 1024;
      case 'MiB':
        return value * 1024 * 1024;
      case 'GiB':
        return value * 1024 * 1024 * 1024;
      default:
        return value;
    }
  }

  /**
   * 重置解析器状态
   */
  reset(): void {
    this.duration = null;
    this.progress = {};
  }
}

// 导出单例
export const progressParser = new ProgressParser();
```

---

## 四、分阶段实施方案

### 阶段 1: FFmpeg 参数生成器 (2-3 周)

#### 4.1 目标
- 创建 FFmpeg 参数生成模块
- 实现基础的视频/音频编码参数
- 提供命令行预览功能

#### 4.2 技术要点

| 要点 | 实现方式 | 优先级 |
|------|---------|--------|
| **类型定义** | 创建完整的 TypeScript 接口 | P0 |
| **编码器数据库** | JSON 文件存储编码器配置 | P0 |
| **命令行生成** | 参考预设管理.vb 逻辑 | P0 |
| **基础 UI 组件** | 配置面板、命令预览 | P0 |
| **参数验证** | 输入验证和错误提示 | P1 |

#### 4.3 实施步骤

1. **Week 1: 基础架构**
   - [ ] 创建 `src/ffmpeg` 目录结构
   - [ ] 定义 TypeScript 类型 (`types/preset.ts`, `types/encoder.ts`)
   - [ ] 实现编码器数据库 (`data/encoders.ts`)
   - [ ] 创建 Pinia Store (`stores/ffmpegParams.ts`)

2. **Week 2: 命令行生成**
   - [ ] 实现命令行生成服务 (`services/commandBuilder.ts`)
   - [ ] 支持基础视频编码参数
   - [ ] 支持基础音频编码参数
   - [ ] 实现参数转义和路径处理

3. **Week 3: UI 组件**
   - [ ] 创建主配置面板 (`FFmpegConfigPanel.vue`)
   - [ ] 创建视频编码器选项卡 (`VideoEncoderTab.vue`)
   - [ ] 创建音频编码器选项卡 (`AudioEncoderTab.vue`)
   - [ ] 创建命令行预览组件 (`CommandPreview.vue`)
   - [ ] 集成到 Rebebuca 主界面

#### 4.4 验收标准

- [ ] 可以选择视频编码器并生成正确的命令行
- [ ] 可以选择音频编码器并生成正确的命令行
- [ ] 命令行预览正确显示参数
- [ ] 所有基础参数验证正常工作
- [ ] 通过单元测试覆盖率 80%+

### 阶段 2: 进度解析器 (1 周)

#### 4.1 目标
- 集成 FFmpeg 进度解析器
- 实现实时进度显示
- 添加剩余时间和预估大小计算

#### 4.2 技术要点

| 要点 | 实现方式 | 优先级 |
|------|---------|--------|
| **进度解析** | 正则表达式提取 | P0 |
| **实时更新** | 通过终端事件监听 | P0 |
| **进度计算** | 基于时间计算百分比 | P0 |
| **错误检测** | 识别 FFmpeg 错误输出 | P0 |
| **状态管理** | 集成到 Terminal Store | P1 |

#### 4.3 实施步骤

1. **Week 1: 进度解析服务**
   - [ ] 实现进度解析服务 (`services/progressParser.ts`)
   - [ ] 解析 Duration 信息
   - [ ] 解析 frame, fps, q, size, time, bitrate, speed
   - [ ] 计算进度百分比和剩余时间

2. **集成到终端**
   - [ ] 扩展 Terminal Store，支持 FFmpeg 进度
   - [ ] 在输出处理中调用解析器
   - [ ] 创建进度监控组件 (`ProgressMonitor.vue`)
   - [ ] 显示进度条、帧率、速度等信息

#### 4.4 验收标准

- [ ] 实时进度显示准确
- [ ] 进度百分比计算正确
- [ ] 剩余时间估算合理
- [ ] 能正确识别 FFmpeg 错误
- [ ] 进度信息显示美观直观

### 阶段 3: 预设系统 (1-2 周)

#### 4.1 目标
- 实现预设管理功能
- 支持导入/导出 3FUI 预设
- 提供预设模板库

#### 4.2 技术要点

| 要点 | 实现方式 | 优先级 |
|------|---------|--------|
| **预设存储** | Tauri Storage API | P0 |
| **预设管理 UI** | 预设列表、编辑器 | P0 |
| **3FUI 兼容** | 字段映射转换 | P0 |
| **预设模板** | 内置常用预设 | P1 |
| **批量导入** | 支持多个预设导入 | P1 |

#### 4.3 实施步骤

1. **Week 1: 预设存储**
   - [ ] 实现预设管理 Store (`stores/ffmpegPresets.ts`)
   - [ ] 实现预设保存/加载/删除
   - [ ] 实现 3FUI 格式导入
   - [ ] 实现 3FUI 格式导出

2. **Week 2: 预设管理 UI**
   - [ ] 创建预设管理器组件 (`PresetManager.vue`)
   - [ ] 创建预设编辑器
   - [ ] 实现预设模板库
   - [ ] 添加预设导入/导出功能

#### 4.4 验收标准

- [ ] 可以保存和加载自定义预设
- [ ] 可以从 3FUI 导入预设文件
- [ ] 预设字段映射正确
- [ ] 提供不少于 10 个内置预设模板
- [ ] 预设管理 UI 易用性良好

### 阶段 4: 高级功能 (2-3 周)

#### 4.1 目标
- 实现视频滤镜支持
- 实现色彩管理
- 实现剪辑区间
- 实现二遍编码

#### 4.2 技术要点

| 要点 | 实现方式 | 优先级 |
|------|---------|--------|
| **视频滤镜** | FFmpeg -vf 参数生成 | P0 |
| **裁剪/缩放** | 滤镜链支持 | P0 |
| **字幕烧录** | subtitles 滤镜 | P1 |
| **色彩管理** | format/colormatrix | P1 |
| **二遍编码** | -pass 1/2 逻辑 | P1 |
| **剪辑区间** | -ss/-to 参数 | P0 |

#### 4.3 实施步骤

1. **Week 1: 基础滤镜**
   - [ ] 实现裁剪滤镜 UI 和逻辑
   - [ ] 实现缩放滤镜 UI 和逻辑
   - [ ] 实现帧率转换
   - [ ] 实现去隔行

2. **Week 2: 高级滤镜**
   - [ ] 实现字幕烧录
   - [ ] 实现色彩管理
   - [ ] 实现降噪和锐化
   - [ ] 实现旋转/翻转

3. **Week 3: 其他高级功能**
   - [ ] 实现剪辑区间
   - [ ] 实现二遍编码
   - [ ] 实现流映射控制
   - [ ] 创建滤镜面板组件 (`FilterPanel.vue`)

#### 4.4 验收标准

- [ ] 所有基础滤镜正常工作
- [ ] 字幕烧录正确显示
- [ ] 色彩管理不影响画质
- [ ] 剪辑区间准确
- [ ] 二遍编码质量提升明显

### 阶段 5: 优化完善 (1-2 周)

#### 4.1 目标
- 性能优化
- 用户体验优化
- 文档和教程
- 错误处理完善

#### 4.2 技术要点

| 要点 | 实现方式 | 优先级 |
|------|---------|--------|
| **性能优化** | 减少重渲染、虚拟滚动 | P0 |
| **简单模式** | 隐藏高级选项 | P0 |
| **专家模式** | 显示所有参数 | P1 |
| **批量处理** | 支持多文件队列 | P0 |
| **错误处理** | 友好的错误提示 | P0 |
| **文档** | 参数说明和示例 | P1 |

#### 4.3 实施步骤

1. **Week 1: 性能和体验**
   - [ ] 优化大列表渲染性能
   - [ ] 实现简单/专家模式切换
   - [ ] 优化命令行生成速度
   - [ ] 添加批量处理功能

2. **Week 2: 文档和测试**
   - [ ] 编写用户文档
   - [ ] 创建视频教程
   - [ ] 完善错误处理
   - [ ] 编写单元测试和集成测试

#### 4.4 验收标准

- [ ] 应用响应流畅，无明显卡顿
- [ ] 简单模式易上手，专家模式功能完整
- [ ] 批量处理高效稳定
- [ ] 错误提示清晰友好
- [ ] 文档完整易懂

---

## 五、与现有架构的集成

### 5.1 Task Manager 扩展

```typescript
// stores/taskManager.ts (修改)

// 扩展 Task 类型
export interface FFmpegTask extends Task {
  type: 'ffmpeg';
  ffmpegConfig: FFmpegPreset;
  inputFiles: string[];
  outputFile?: string;
  progress?: FFmpegProgress;
}

// 添加 FFmpeg 任务执行
async function executeFFmpegTask(task: FFmpegTask): Promise<void> {
  const terminalStore = useTerminalStore();
  const ffmpegStore = useFFmpegParamsStore();

  // 生成命令行
  const command = await commandBuilder.build(
    task.ffmpegConfig,
    task.inputFiles[0],
    task.outputFile || generateOutputPath(task.inputFiles[0], task.ffmpegConfig)
  );

  // 解析命令
  const parsed = parseCommandLine(command);

  // 创建终端 tab 执行任务
  const tab = await terminalStore.executeTask({
    command: parsed.command,
    args: parsed.args,
    cwd: task.cwd,
    taskId: task.id,
    label: task.name,
  });

  // 存储进度监听器引用
  tab.ffmpegProgressListener = progressParser;
}
```

### 5.2 Terminal Store 扩展

```typescript
// stores/terminal.ts (修改)

// 扩展 Tab 类型
interface TerminalTab {
  // ... 现有字段

  // FFmpeg 扩展字段
  ffmpegTask?: boolean;
  ffmpegProgress?: FFmpegProgress;
  ffmpegProgressListener?: ProgressParser;
}

// 在输出处理中集成 FFmpeg 进度解析
function handleOutput(ptyId: string, data: string) {
  const tab = tabs.value.find(t => t.ptyId === ptyId);
  if (!tab) return;

  // 常规输出处理
  tab.output += data;

  // FFmpeg 进度解析
  if (tab.ffmpegTask && tab.ffmpegProgressListener) {
    const event = tab.ffmpegProgressListener.parse(data);
    if (event) {
      handleFFmpegProgressEvent(tab, event);
    }
  }
}

// 处理 FFmpeg 进度事件
function handleFFmpegProgressEvent(tab: TerminalTab, event: FFmpegProgressEvent) {
  switch (event.type) {
    case 'progress':
      tab.ffmpegProgress = event.data;
      break;
    case 'complete':
      tab.status = 'idle';
      break;
    case 'error':
      tab.status = 'error';
      tab.error = event.error;
      break;
    case 'duration':
      // 存储总时长用于计算进度
      break;
  }
}
```

### 5.3 Rust 后端扩展 (可选)

如果需要在 Rust 后端添加特定支持：

```rust
// src-tauri/src/ffmpeg.rs

use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize)]
pub struct FFmpegPreset {
    pub output: OutputConfig,
    pub video: VideoConfig,
    pub audio: AudioConfig,
    // ... 其他字段
}

#[tauri::command]
pub async fn save_preset(key: String, value: String) -> Result<(), String> {
    // 使用 Tauri Store API 保存预设
    Ok(())
}

#[tauri::command]
pub async fn load_preset(key: String) -> Result<String, String> {
    // 从 Tauri Store 加载预设
    Ok(String::new())
}

#[tauri::command]
pub async fn list_preset_keys(prefix: String) -> Result<Vec<String>, String> {
    // 列出所有预设键
    Ok(vec![])
}

#[tauri::command]
pub async fn delete_preset(key: String) -> Result<(), String> {
    // 删除预设
    Ok(())
}
```

---

## 六、技术风险评估与应对

### 6.1 技术风险

| 风险 | 等级 | 影响 | 应对措施 |
|------|------|------|---------|
| **命令行生成错误** | 高 | 转码失败 | 充分测试、参数验证、命令行验证工具 |
| **进度解析不准确** | 中 | 用户体验差 | 多版本 FFmpeg 测试、容错处理 |
| **性能问题** | 中 | 大文件卡顿 | 虚拟滚动、Web Worker、进度采样 |
| **跨平台兼容性** | 中 | 某些平台不可用 | 充分测试、条件编译、降级方案 |
| **UI 复杂度高** | 中 | 用户难以上手 | 简单/专家模式、引导教程 |

### 6.2 业务风险

| 风险 | 等级 | 影响 | 应对措施 |
|------|------|------|---------|
| **功能缺失** | 中 | 不如 3FUI 完善 | 分阶段实施、用户反馈优先级 |
| **用户习惯差异** | 低 | 3FUI 用户不适应 | 提供兼容模式、文档迁移指南 |
| **维护成本高** | 低 | 长期维护困难 | 模块化设计、充分文档、测试覆盖 |

### 6.3 时间风险

| 风险 | 等级 | 影响 | 应对措施 |
|------|------|------|---------|
| **开发延期** | 中 | 无法按时交付 | MVP 优先、分阶段交付 |
| **技术调研不充分** | 低 | 返工重做 | 充分调研、原型验证 |
| **第三方依赖变更** | 低 | 兼容性问题 | 锁定版本、持续监控 |

---

## 七、测试策略

### 7.1 单元测试

```
src/ffmpeg/services/__tests__/
├── commandBuilder.spec.ts     # 命令行生成测试
├── progressParser.spec.ts     # 进度解析测试
└── encoderDatabase.spec.ts    # 编码器数据库测试
```

### 7.2 集成测试

```
src/ffmpeg/__tests__/
├── ffmpegIntegration.spec.ts  # FFmpeg 模块集成测试
└── presetImport.spec.ts       # 预设导入导出测试
```

### 7.3 E2E 测试

```
tests/e2e/ffmpeg/
├── basicEncoding.spec.ts      # 基础编码测试
├── filterPipeline.spec.ts     # 滤镜管道测试
├── presetManagement.spec.ts   # 预设管理测试
└── batchProcessing.spec.ts    # 批量处理测试
```

---

## 八、性能优化建议

### 8.1 前端优化

1. **虚拟滚动**: 使用 `vue-virtual-scroller` 处理大文件列表
2. **懒加载**: 组件按需加载
3. **防抖节流**: 输入框使用防抖
4. **Memoization**: 缓存计算结果

### 8.2 命令行生成优化

1. **参数缓存**: 缓存常用参数组合
2. **惰性生成**: 只在需要时生成命令行
3. **增量更新**: 只更新变化的参数部分

### 8.3 进度解析优化

1. **采样率**: 降低解析频率 (例如每 100ms 解析一次)
2. **批量更新**: 批量更新 UI 而非每次输出都更新
3. **Web Worker**: 将解析逻辑移至 Worker

---

## 九、部署和发布计划

### 9.1 版本规划

| 版本 | 功能 | 预计时间 |
|------|------|---------|
| v0.1.0 | 阶段 1: 参数生成器 | 2-3 周 |
| v0.2.0 | 阶段 2: 进度解析器 | 1 周 |
| v0.3.0 | 阶段 3: 预设系统 | 1-2 周 |
| v0.4.0 | 阶段 4: 高级功能 | 2-3 周 |
| v1.0.0 | 阶段 5: 优化完善 | 1-2 周 |

### 9.2 发布策略

1. **Beta 测试**: 每个阶段完成后邀请用户测试
2. **灰度发布**: 首先发布给部分用户
3. **全量发布**: 稳定后全量发布
4. **用户反馈**: 收集反馈持续改进

---

## 十、总结

### 10.1 设计亮点

1. **渐进式融合**: 分 5 个阶段逐步实施，风险可控
2. **模块化设计**: 功能解耦，便于维护和扩展
3. **兼容性优先**: 保持与 Rebebuca 现有架构的兼容性
4. **用户体验**: 简单模式 + 专家模式，满足不同用户需求

### 10.2 技术栈

| 层级 | 技术选型 |
|------|---------|
| **前端** | Vue 3 + TypeScript + Naive UI |
| **状态管理** | Pinia |
| **后端** | Tauri 2 + Rust |
| **命令执行** | PTY (通过 Tauri) |
| **数据存储** | Tauri Store |
| **构建工具** | Vite |

### 10.3 后续展望

1. **云端渲染**: 支持云服务器转码
2. **AI 优化**: AI 自动优化编码参数
3. **插件系统**: 支持第三方滤镜和编码器
4. **协作编辑**: 多人协作预设配置

---

**文档结束**

**审核状态**: 待审核
**审核人**: ___________
**审核日期**: ___________
