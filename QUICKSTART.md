# 快速入门指南

欢迎使用 Rebebuca！这是一个快速入门指南，帮助你在 5 分钟内掌握基本功能。

## 🚀 启动应用

```bash
# 开发模式
pnpm tauri:dev

# 或者构建生产版本
pnpm tauri build
```

## 📝 第一个运行配置

### 方法 1：使用预置配置

应用已经包含两个示例配置：
1. **示例配置 - Echo**: 简单的 echo 命令
2. **示例配置 - 列出文件**: ls 命令列出文件

点击配置旁边的 **▶️ 运行按钮** 即可立即执行！

### 方法 2：创建新配置

1. 点击左侧顶部的 **"新建"** 按钮
2. 填写配置信息：

   **示例 1: Hello World**
   ```
   配置名称: Hello World
   执行命令: echo
   命令行参数: 
     - Hello, World!
   ```

   **示例 2: 列出目录**
   ```
   配置名称: 列出当前目录
   执行命令: ls
   命令行参数:
     - -la
   工作目录: /Users/yourname
   ```

   **示例 3: Python 脚本**
   ```
   配置名称: 运行 Python
   执行命令: python3
   命令行参数:
     - --version
   ```

3. 点击 **"保存"**
4. 点击配置旁边的 **▶️ 运行按钮**

## 🎮 基本操作

### 运行配置
- 点击配置右侧的 **▶️ 播放图标**
- 会自动创建一个新的 tab 并显示输出

### Tab 工具栏按钮

| 按钮 | 功能 | 说明 |
|------|------|------|
| 🔄 | 重启 | 使用相同配置重新运行 |
| ⏹️ | 停止 | 终止当前运行的进程 |
| 🗑️ | 清空 | 清空当前 tab 的输出 |
| ⬇️ | 滚动 | 滚动到输出底部 |
| ✏️ | 编辑 | 编辑当前配置 |

### Tab 状态指示

每个 tab 名称前的圆点颜色表示状态：
- 🟢 **绿色** = 进程正在运行
- 🔵 **蓝色** = 进程成功完成
- 🔴 **红色** = 进程出错或被终止
- ⚪ **灰色** = 空闲状态

### 运行历史

右侧面板显示运行历史：
- 点击 **🔄** 重新运行历史记录
- 点击 **🗑️** 删除历史记录
- 点击顶部的 **🗑️** 清空所有历史

## 📚 实用示例

### 1. 监控日志文件

```
命令: tail
参数: 
  - -f
  - /path/to/logfile.log
```

### 2. 运行开发服务器

```
命令: npm
参数:
  - run
  - dev
工作目录: /path/to/your/project
```

### 3. 运行 Python 脚本

```
命令: python3
参数:
  - script.py
  - --option
  - value
工作目录: /path/to/your/project
```

### 4. 带环境变量的命令

```
命令: node
参数:
  - server.js
环境变量:
  - NODE_ENV = production
  - PORT = 3000
```

### 5. 编译项目

```
命令: make
工作目录: /path/to/your/project
```

## 🧪 测试功能

项目包含测试脚本，可以快速验证功能：

```bash
# 1. 赋予执行权限
chmod +x test_scripts/*.sh

# 2. 在 Rebebuca 中创建配置
命令: bash
参数: test_scripts/test_counter.sh
工作目录: (项目根目录)

# 3. 运行并测试
- 观察实时输出
- 测试停止按钮
- 测试重启功能
```

## 💡 使用技巧

### 技巧 1: 命令和参数分离
❌ **错误做法:**
```
命令: echo "Hello World"
```

✅ **正确做法:**
```
命令: echo
参数: 
  - Hello World
```

### 技巧 2: 路径处理
- 使用绝对路径更可靠
- 或者设置正确的工作目录

### 技巧 3: 长运行进程
- 对于持续运行的进程（如开发服务器）
- 使用"停止"按钮优雅地终止
- 不要直接关闭 tab

### 技巧 4: 多任务并行
- 可以同时运行多个配置
- 每个配置在独立的 tab 中
- 互不干扰

### 技巧 5: 快速访问历史
- 右侧历史面板可以快速重新运行
- 无需重新配置

## 🔧 故障排除

### 问题: "命令未找到"
**原因**: 命令不在系统 PATH 中  
**解决**: 使用命令的完整路径，如 `/usr/bin/python3`

### 问题: 没有输出显示
**原因**: 程序可能还没有输出或缓冲输出  
**解决**: 等待几秒，或检查程序是否正确执行

### 问题: 无法停止进程
**原因**: 某些进程可能无法通过标准方式终止  
**解决**: 使用系统任务管理器手动终止

### 问题: 工作目录错误
**原因**: 指定的目录不存在或无权限  
**解决**: 检查目录路径是否正确，确保有访问权限

## 📖 更多信息

- 详细实现说明: [IMPLEMENTATION_NOTES.md](IMPLEMENTATION_NOTES.md)
- 更新日志: [CHANGELOG.md](CHANGELOG.md)
- 测试脚本: [test_scripts/README.md](test_scripts/README.md)

## 🎯 下一步

现在你已经掌握了基础！尝试：
1. 创建你自己的常用命令配置
2. 尝试运行你的开发服务器
3. 配置你的构建脚本
4. 探索更多高级功能

祝你使用愉快！🎉

