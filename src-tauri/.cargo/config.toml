[env]
# 禁用 clang 模块功能以解决 macOS 15 (Sequoia) 的兼容性问题
CFLAGS = "-fno-modules"
CXXFLAGS = "-fno-modules"
OBJCFLAGS = "-fno-modules"
# OpenSSL 配置，用于 openssl-sys crate 编译
# 本地开发环境默认路径（Apple Silicon）
# 在 CI 环境中，OPENSSL_DIR 会通过环境变量覆盖此设置
OPENSSL_DIR = "/opt/homebrew/opt/openssl@3"

